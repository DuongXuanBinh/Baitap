#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

struct dodai{
	float yards;
	float feet;
	float inches;
};

void main(){
	struct dodai a[5];
	void sort(struct leng b[5]);
	for(i=0;i<5;i++)
		printf("Nhap do dai thu %d",i+1);
		scanf("%d",)
}
void sort(struct leng b[5]){
	int i,j;
	for(i=0;i<4;i++);
		for(j=i+1;j<5,j++)
			if
}

